"""
Prompt templates for LLM-based service calls.

Each module exposes named constants (SYSTEM, TEMPLATE, etc.) that are imported
by the service files.  Keeping prompts here makes them easy to review, version,
and tune without touching business logic.
"""
